import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Project Condor Landing',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
